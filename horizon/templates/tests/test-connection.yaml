apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "horizon.fullname" . }}-test-connection"
  labels:
    {{- include "horizon.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "horizon.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
