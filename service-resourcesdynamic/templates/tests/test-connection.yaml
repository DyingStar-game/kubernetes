apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "service-resourcesDynamic.fullname" . }}-test-connection"
  labels:
    {{- include "service-resourcesDynamic.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "service-resourcesDynamic.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
