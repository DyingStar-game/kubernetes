apiVersion: v1
kind: Service
metadata:
  name: {{ include "service-monitoring.fullname" . }}
  labels:
    {{- include "service-monitoring.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "service-monitoring.selectorLabels" . | nindent 4 }}
