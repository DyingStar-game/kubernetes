# Production overrides for service-monitoring.
image:
  repository: harbor.dyingstar-game.space/dyingstar/service-monitoring
  pullPolicy: IfNotPresent

serviceMonitor:
  enabled: true

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    memory: 128Mi
