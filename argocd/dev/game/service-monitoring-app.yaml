apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: service-monitoring
  namespace: argocd
spec:
  project: game
  source:
    repoURL: 'file:///mnt/local-repo.git'
    path: service-monitoring
    targetRevision: HEAD
    helm:
      valueFiles:
        - $values/service-monitoring/values-dev.yaml
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: dyingstar
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true