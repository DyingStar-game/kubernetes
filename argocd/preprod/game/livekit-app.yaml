apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: livekit
  namespace: argocd
spec:
  project: game
  source:
    repoURL: 'https://github.com/dyingstar-game/kubernetes.git'
    path: livekit
    targetRevision: main
    helm:
      valueFiles:
        - $values/livekit/values-preprod.yaml
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: dyingstar-preprod
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true