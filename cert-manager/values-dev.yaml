installCRDs: true
extraArgs:
  - --enable-gateway-api

extraObjects:
  - |
    apiVersion: cert-manager.io/v1
    kind: ClusterIssuer
    metadata:
      name: local-selfsigned-issuer
    spec:
      selfSigned: {}