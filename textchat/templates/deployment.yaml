{{- if .Values.mosquitto.enabled }}
{{- $jwt := eq .Values.mosquitto.auth.mode "jwt" }}
{{- $image := ternary .Values.mosquitto.goauthImage .Values.mosquitto.image $jwt }}
{{- $confPath := ternary "/etc/mosquitto/mosquitto.conf" "/mosquitto/config/mosquitto.conf" $jwt }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "textchat.fullname" . }}-mosquitto
  labels:
    {{- include "textchat.labels" . | nindent 4 }}
    app.kubernetes.io/component: mosquitto
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "textchat.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: mosquitto
  template:
    metadata:
      annotations:
        # Roll the pod when the config changes.
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
      labels:
        {{- include "textchat.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: mosquitto
    spec:
      serviceAccountName: {{ include "textchat.serviceAccountName" . }}
      containers:
        - name: mosquitto
          image: "{{ $image.repository }}:{{ $image.tag }}"
          imagePullPolicy: {{ $image.pullPolicy }}
          ports:
            - name: mqtt
              containerPort: {{ .Values.mosquitto.service.mqttPort }}
              protocol: TCP
            - name: websocket
              containerPort: {{ .Values.mosquitto.service.websocketPort }}
              protocol: TCP
          securityContext:
            runAsUser: 1883
            runAsGroup: 1883
          volumeMounts:
            - name: config
              mountPath: {{ $confPath }}
              subPath: mosquitto.conf
          {{- with .Values.mosquitto.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          livenessProbe:
            tcpSocket:
              port: mqtt
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            tcpSocket:
              port: websocket
            initialDelaySeconds: 5
            periodSeconds: 5
      volumes:
        - name: config
          configMap:
            name: {{ include "textchat.fullname" . }}-mosquitto
            defaultMode: 0644
{{- end }}
